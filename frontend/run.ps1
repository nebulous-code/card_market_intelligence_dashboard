# Starts the Vite development server for the Vue frontend on port 5151.
#
# Vite watches for file changes and hot-reloads the browser automatically,
# so edits to Vue components appear in the browser without a manual refresh.
#
# The frontend will be available at http://127.0.0.1:5151
#
# Note: The API server must be running on port 8151 before starting the
# frontend, otherwise the dashboard will show an error when it tries to
# load set data.
#
# Usage: .\run.ps1

npm run dev
